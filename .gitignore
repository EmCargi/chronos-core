# Confidential character data — roster DBs, checkpoints, runtime log
data/

# Confidential character-specific narrative content (scenario packages
# reference named characters e.g. Tomoe, Nieven — same category as data/)
modules/

# Confidential processed character profiles awaiting/after ingest
staging/

# Archived session/design docs — historical, not part of the active engine
archives/

# One-shot migration script with character backstory/psychology hardcoded
# as string literals (sixth guard, personas, narrative levers) — confidential
# IP, not just a name reference. Also flagged as root clutter in the handoff.
# backfill_besm.py archived to dev/archive/chronos-core/ (2026-08-14)
# Virtual environment
venv/
.venv/

# Python bytecode & caches
__pycache__/
*.pyc
.pytest_cache/

# OS files
.DS_Store
Thumbs.db

# Editor temp
*.swp
*.swo
*~
*.bak
